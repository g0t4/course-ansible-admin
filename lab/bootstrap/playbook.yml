# yaml-language-server: $schema=https://raw.githubusercontent.com/ansible/ansible-lint/refs/heads/main/src/ansiblelint/schemas/playbook.json

- name: common packages
  hosts: server*
  become: true
  tasks:
    # TODO? first demo leave inetutils uninstalled (show error) 
    # - resolve with inventory pointed at vagrant VMs!
    - name: tools
      pacman:
        name: 
        - inetutils
        - lsd
        - python 
        # for testing mcp-server-commands:
        # - nodejs-lts-iron
        # - npm
        # - extra/typescript
        # - zsh
        state: present
        update_cache: yes # update when can't resolve files anymore
        # git clone https://github.com/g0t4/mcp-server-commands
        # cd ~/mcp-server-commands 
        # npm install

