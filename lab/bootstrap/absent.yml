# yaml-language-server: $schema=https://raw.githubusercontent.com/ansible/ansible-lint/refs/heads/main/src/ansiblelint/schemas/playbook.json

- name: Remove Ubuntu packages for install demo
  hosts: ubuntu*
  become: true
  tasks:
    - name: Remove demo packages
      ansible.builtin.apt:
        name:
          # each command individually:
          - inetutils-telnet
          - inetutils-ping
          - inetutils-traceroute
          #
          - bat
          - ripgrep
          - silversearcher-ag # diff name for ag command
        state: absent


- name: Remove Arch packages for install demo
  hosts: arch*
  become: true
  tasks:
    - name: Remove demo packages
      community.general.pacman:
        name:
          - inetutils # hostname missing b/c of this
          - bat
          - ripgrep
          - the_silver_searcher # diff name for ag command
        state: absent

